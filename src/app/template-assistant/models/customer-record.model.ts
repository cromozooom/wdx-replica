/**
 * Customer data record for template merging.
 * Generic record that supports any flat JSON structure.
 * Fields are dynamically extracted from JSON data at runtime.
 */
export type CustomerRecord = Record<string, any>;
