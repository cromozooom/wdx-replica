/**
 * Represents an available option in the status dropdown selector.
 * Defines the selectable values for the ng-select dropdown.
 */
export interface StatusOption {
  /** Unique identifier */
  id: string;

  /** Display text */
  label: string;
}
