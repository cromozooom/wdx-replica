
[build]
command = "pip install faker && python scripts/generate-mock-data.py && npm install --legacy-peer-deps && npm run build"
publish = "dist/automation-app/browser"

[build.environment]
NPM_FLAGS = "--legacy-peer-deps"
